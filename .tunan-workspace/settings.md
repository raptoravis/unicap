---
current_sprint: SPT-001
---

# tunan workspace settings

To switch sprint: change `current_sprint` to the next `SPT-NNN` and create that sprint directory plus its `reqs/` subdirectory under `sprints/` (with a `.gitkeep`).
